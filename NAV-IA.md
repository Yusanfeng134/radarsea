# 出海雷达 · 导航栏信息架构(IA)梳理

> **从"数据品牌"和"快速商业化"两个角度同时压力测试现有导航,得出一份可以直接拿去改代码的新结构。**
>
> 配套文档:`DESIGN.md`(怎么做)/ `ROADMAP.md`(做什么)/ **`NAV-IA.md`(怎么排)**

---

## 一、当前导航在哪些点上漏血

### 现状

```
[Logo] 出海雷达    [产品 ▾] [解决方案 ▾] [行业洞察] [关于]    [中] [登录] [联系销售]
```

5 项一级导航 + 3 个右侧动作。

### 三个明显的漏血点

#### 1. 数据品牌身份没立住

数据品牌的核心差异化是**数据本身**。但当前一级导航里完全没有指向"我们的数据"的入口:
- 1 亿+ IP 数据 — 在哪能看?
- 156 国家覆盖 — 在哪能验证?
- USPTO / EUIPO / CNIPA 政府数据库接入 — 在哪能查?
- 安全合规 / 数据驻留 / GDPR — 在哪能下载报告?

结果:**客户把出海雷达当一般工具,而不是当一手数据源**。

#### 2. 转化漏斗中段全是空洞

按典型 B2B SaaS 买家旅程对照:

```
[认知] → [兴趣] → [评估] → [试用] → [决策] → [复购]
   ↑        ↑        ↑        ↑        ↑        ↑
   ✓        ✓        ❌       ❌       ❌       ❌
```

- **评估阶段**:没有 `/pricing`、没有 `/customers`、没有信任中心 — 中型客户走到这里就掉了
- **试用阶段**:Detection 雷达已上线,但**整个网站找不到"注册/试用"的入口** — 这是 Detection 唯一的产品价值出口
- **决策阶段**:没有客户案例 / ROI 证明
- **复购阶段**:没有 changelog / 没有产品更新

漏斗中段的页面缺口在 `ROADMAP.md` 里已经详细列过。**这里要补的是导航层面让这些页面有一级入口**。

#### 3. 主 CTA 错位

唯一的顶部主 CTA 是 [联系销售]。但 Detection 雷达**已上线**,意味着至少有两类买家:

| 买家类型 | 决策路径 | 想要的 CTA |
|---|---|---|
| **自助型(中小卖家 / 个人选品)** | 自己试用 → 见效 → 升级 | "开始免费试用" |
| **企业型(品牌方 / SaaS / 大客户)** | 联系销售 → 演示 → POC → 签合同 | "联系销售" |

**只有联系销售一个 CTA = 完全放弃了自助型客户**。这部分客户在中国跨境圈子里数量比企业型还多 — 他们看到没有自助入口就直接走了,不会发起销售对话。

---

## 二、改造原则

### 原则 A · 数据品牌优先(Data-first nav)

> "我们的数据本身就是产品。"

把"数据"提升到一级导航。数据底座、覆盖地图、信任中心、方法论 — 这四个页面合在一个 `数据 ▾` 大菜单里,**直接对外宣布"我们是数据公司"**。

### 原则 B · 漏斗对齐(Funnel-aligned nav)

> 一级导航的每一项,都要对应漏斗里的一个阶段。

| 漏斗阶段 | 一级入口 | 这个入口要解决的问题 |
|---|---|---|
| 兴趣 | 产品 | "你们卖什么" |
| 评估 1 — 数据 | 数据 | "你们数据真不真" |
| 评估 2 — 信任 | 客户 | "别人用了之后好了多少" |
| 试用决策 | 定价 | "我能负担得起吗 / 适合我吗" |
| 长尾 / 教育 | 资源 | "我能学到东西吗" |

5 个阶段 = 5 个一级入口,对齐。

### 原则 C · 双 CTA 通道(Two-track CTA)

> 自助一条路,销售一条路,不要把两条挤在一起。

- 顶部主 CTA: **[开始试用 →]** (低门槛,服务自助型客户)
- 在 hero / pricing Enterprise / bottom CTA 上保留 **[联系销售]** (高门槛,服务企业型客户)
- 弹窗(已建好的 `useContactSales`)继续作为联系销售的承接

---

## 三、推荐的新导航结构

### 桌面端(`lg+`)

```
[Logo] 出海雷达    [产品 ▾] [数据 ▾] [客户] [定价] [资源 ▾]    [中] [登录] [开始试用 →]
```

**5 项一级导航 + 3 个右侧动作 = 8 个交互元素**(原来也是 8 个,数量持平,但每一个都更高效)。

| 入口 | 类型 | 链接 | 何时展开 |
|---|---|---|---|
| **产品** | Mega menu | `/products` | 5 款雷达 + 全局 Demo |
| **数据** | Mega menu | `/data-sources`(默认) | 4 列:底座 / 覆盖 / 信任 / 方法论 |
| **客户** | 直链 | `/customers` | — (一级即落地) |
| **定价** | 直链 | `/pricing` | — (一级即落地) |
| **资源** | Mega menu | `/insights`(默认) | 4 列:洞察 / 报告 / 文档 / 公司 |

### 右侧动作

| 动作 | 行为 | 视觉 |
|---|---|---|
| 中/EN | 语言切换 | 文字 + globe icon(保留现状) |
| 登录 | 跳转 `console.overseasradar.com` | 文字链 |
| **开始试用 →** | 跳转 `/signup` 或 `https://detection.overseasradar.com/signup` | **白底深字 pill 按钮(沿用 Hero CTA 的样式)** |

---

## 四、每个 Mega Menu 的二级内容详解

### 4.1 产品 ▾ (保留 Phase 12 已重构的 5+1 mega menu)

不需要改动。最近一次重构后已经是:
- **列 1 — 检测雷达** (Live · 整列可点击)
- **列 2-5 — 选品/品牌/合规/舆情雷达** (frozen · `[ Status: 研发中 ]` tooltip)
- **列 6 — 全局 Demo** (`将出海数据,转化为确定性增长` · CTA beam 按钮)

### 4.2 数据 ▾ (新增 · 数据品牌的差异化心脏)

按"数据可信度"的四个层级铺排:

| 列 | 标题 | 路径 | 一句话定位 |
|---|---|---|---|
| 1 | **数据底座** | `/data-sources` | 1 亿+ IP / 500+ 平台 / 政府数据库清单与刷新频率 |
| 2 | **覆盖地图** | `/coverage` | 156 国家可视化 + 数据深度三级 |
| 3 | **信任中心** | `/trust` | SOC 2 / GDPR / 子处理者 / 渗透测试 |
| 4 | **方法论** | `/methodology` | AI 评分如何计算 / 模型准确率 / 局限性 |

**第 5 列(可选 · 数据 demo CTA):** 一个浅色 promo 卡片,"看一次 60 秒数据 demo"或"下载完整数据词典 PDF"。

### 4.3 资源 ▾ (新增 · 把"行业洞察"+"关于"+长尾内容收编)

5 列结构:

| 列 | 标题 | 路径 | 内容 |
|---|---|---|---|
| 1 | **行业洞察** | `/insights` | 博客文章 · 趋势观察 · 合规洞察 |
| 2 | **白皮书 / 报告** | `/reports` | gated PDF · 深度研究 |
| 3 | **开发者文档** | `/developers` | API · SDK · webhook · 状态页链接 |
| 4 | **产品更新** | `/changelog` | 版本日志 · 路线图 |
| 5 | **公司** | — | 关于我们 (`/about`) · 加入我们 (`/careers`) · 联系我们 (`/contact`) |

> 把"关于"从一级降到二级是数据品牌的常见做法。Linear / Vercel / Stripe 都是这样。**首页能讲清楚的故事,不需要把公司本身放到一级**。

### 4.4 客户 / 定价 — 不需要 mega menu

直接链接到目标页。展开 mega menu 反而稀释这两个页面的转化意图 — 这两个页面的目标就是"看到 → 阅读 → 行动",不要让 hover 出现的 mega menu 截胡。

---

## 五、被移除项的去向 · FAQ

### Q1: 一级"解决方案"去哪了?

**答:** 移除一级独立位,但**不删除 `/solutions` 页面本身**。两种入口保留:
1. 首页的 `<SolutionsPreview />` 组件(已有,tabs+detail 形式)
2. 在 **资源 ▾ → 行业洞察** 的 mega menu 角落加一个 "按角色找方案 →" 的快捷入口

**为什么:** "解决方案"在数据品牌网站上是一个**话术页面**,不是产品页面。它服务于销售环节("我应该向客户介绍哪个雷达组合"),不应该和真正的产品/数据并列在一级。

### Q2: 一级"行业洞察"去哪了?

**答:** 沉到 **资源 ▾** 的第 1 列。原页面 `/insights` 不变,只是入口位置变。

**为什么:** "行业洞察"是教育型内容,不是转化型内容。它属于"长尾 / 教育"漏斗段,放在资源里更准确。

### Q3: 一级"关于"去哪了?

**答:** 沉到 **资源 ▾** 的第 5 列(公司类目)。

**为什么:** 一级"关于"是中小公司的导航惯性。**数据公司不需要把自己放在一级 — 数据本身就是介绍**。

### Q4: 顶部"联系销售"去哪了?

**答:** 从顶部 CTA 撤下,但**保留**在三个高意向位置:
1. **首页 Hero**(立即体验 + 申请 Demo 双 CTA 之一)
2. **`/pricing` Enterprise 行**(只有 Enterprise 才需要联系销售)
3. **每页底部 BottomCTA**

**为什么:** 顶部 CTA 是访客**第一次看到的转化按钮**。第一次的 CTA 就把人引向"联系销售",等于直接放弃了所有不愿意填表单的访客 — 而这些人占总访客的 80%+。

把顶部 CTA 换成低门槛的 [开始试用],等于让 80% 的访客第一次接触就有路可走;高意向的 20% 在阅读到 hero/pricing/bottom-cta 时,会自然看到联系销售的承接,**两条路并行不冲突**。

### Q5: 顶部"登录"还需要吗?

**答:** 需要,但**位置和优先级要降**。

调整后:
- 视觉上:从原来的"中等显眼"降级到"次要文字链",字号缩小、颜色调到 `text-ink-faint`
- 物理上:仍在右侧,但放在 [开始试用] 的左边,作为低优先级出口

**为什么:** "登录"是为已注册客户准备的;但官网的核心流量是**未注册客户**。视觉权重要让位给主 CTA。

---

## 六、移动端(`<lg`)处理

桌面端的 5 项一级导航,在移动端折叠成单一汉堡菜单。展开后的结构:

```
[X] 关闭
─────
产品
  ↳ 检测雷达 / 选品 / 品牌 / 合规 / 舆情(每条点击直接跳产品官网)
  ↳ 查看完整产品矩阵 →
─────
数据
  ↳ 数据底座
  ↳ 覆盖地图
  ↳ 信任中心
  ↳ 方法论
─────
客户
─────
定价
─────
资源
  ↳ 行业洞察
  ↳ 白皮书/报告
  ↳ 开发者文档
  ↳ 产品更新
  ↳ 关于 / 加入我们 / 联系我们
─────
[开始试用 →]   <- 主 CTA,大尺寸
登录              <- 文字链
中 / EN          <- 切换
```

每个一级类目用现有 `mobileSubOpen` accordion 模式展开,和当前的实现完全兼容。

---

## 七、视觉 / 动效细节(在 `DESIGN.md` 已建立的设计系统内)

### 一级菜单 trigger

- 字号 / weight 与现状一致(`text-sm text-ink-muted hover:text-ink`)
- 带 mega menu 的项加 `<ChevronDown />`(已有逻辑)
- 直链项("客户" / "定价")**不显示 ChevronDown**,避免视觉欺骗

### 主 CTA "开始试用 →"

直接复用 Hero PrimaryCTA 模式:

```jsx
<a
  href="/signup"
  className="group inline-flex h-9 items-center gap-1.5 rounded-full bg-white px-4 text-sm font-medium text-bg transition-all duration-200 hover:bg-white/90 hover:shadow-[0_0_24px_rgba(255,255,255,0.18)]"
>
  开始试用
  <ArrowRight size={14} className="transition-transform duration-300 group-hover:translate-x-0.5" />
</a>
```

### 数据 ▾ mega menu 视觉

沿用 5+1 产品 mega menu 的设计语言:
- 纯黑底 (`bg-bg/92 backdrop-blur-xl`)
- 1px hairline 列分隔
- **数据底座 / 覆盖 / 信任 / 方法论**每列一个**独特的极简线条图标**(stroke-width 1.25),不要再用色块底图
- **第 5 列(可选 demo CTA)**用 `bg-bg-subtle/60` + 滚动数据流暗纹(沿用产品 mega menu 第 6 列的 `code-stream-track`)

### 资源 ▾ mega menu 视觉

更轻巧 — 不需要 5 列分隔的强结构,可以做成更紧凑的列表式:
- 每列只占少量宽度,内容是**一组小链接列表**(如 Linear 资源菜单)
- 顶部加一个最新 1 篇 highlight 文章 / 最新 changelog 摘要

---

## 八、`lib/site.ts` 改动预览

```ts
export type MegaId = "products" | "data" | "resources";

export type NavItem = {
  label: string;
  href: string;
  /** 如果有,hover 触发对应 mega menu */
  megaId?: MegaId;
};

export const primaryNav: NavItem[] = [
  { label: "产品",   href: "/products",   megaId: "products"  },
  { label: "数据",   href: "/data-sources", megaId: "data"     },
  { label: "客户",   href: "/customers"   },
  { label: "定价",   href: "/pricing"     },
  { label: "资源",   href: "/insights",   megaId: "resources" },
];
```

`MegaId` 从 `"products" | "solutions"` 变成 `"products" | "data" | "resources"`(3 个 mega menu 而非 2 个)。

---

## 九、实施 Checklist

按依赖顺序排列:

| # | 任务 | 工时 | 依赖 |
|---|---|---|---|
| 1 | 改 `lib/site.ts` 的 primaryNav + MegaId 类型 | 0.5h | — |
| 2 | 改 `components/site-header.tsx` 的 mega menu switch case | 1h | 1 |
| 3 | 新建 `components/data-mega-menu.tsx`(数据 ▾) | 2-3h | 1 |
| 4 | 新建 `components/resources-mega-menu.tsx`(资源 ▾) | 2h | 1 |
| 5 | 删除 `components/solutions-mega-menu.tsx`(已不再使用) | 0.1h | 2 |
| 6 | 头部主 CTA 从 [联系销售] 换成 [开始试用 →] | 0.5h | 1 |
| 7 | 在 hero / bottom-cta / `/pricing` Enterprise 行保留 [联系销售] 入口 | 已完成 | — |
| 8 | 移动端 drawer 改成新的 5 项 + accordion 展开 | 1.5h | 2 |
| 9 | (并行)目标页面缺哪些建哪些 — 详见 `ROADMAP.md` | 见 roadmap | 独立 |

**纯前端改动小计:大约 1 个工作日**(7-8 小时)。如果再加上 `/pricing` 与 `/customers` 这两个目标页(对开始试用按钮和"客户"导航来说是必须的),总周期约 2-3 个工作日就能让新导航完整可用。

---

## 十、和现有页面的兼容性

| 现有页面 | 在新导航中的入口 | 是否需要修改 |
|---|---|---|
| `/` | Logo | ✅ 不变 |
| `/products` | 产品 ▾ 默认 / "查看完整产品矩阵" 按钮 | ✅ 不变 |
| `/solutions` | 资源 ▾ → 行业洞察的次级入口("按角色找方案") | ❎ 不变,但访问量预期下降 |
| `/insights` | 资源 ▾ → 行业洞察 | ✅ 不变 |
| `/about` | 资源 ▾ → 公司 | ✅ 不变 |
| `/contact` | 资源 ▾ → 公司 / 弹窗承接 | ✅ 不变 |
| `/careers` | 资源 ▾ → 公司 | ✅ 不变 |
| `/privacy` /`/terms` | footer | ✅ 不变 |
| **`/data-sources`** | 数据 ▾ 默认 | ⏳ 需新建 |
| **`/coverage`** | 数据 ▾ → 覆盖地图 | ⏳ 需新建 |
| **`/trust`** | 数据 ▾ → 信任中心 | ⏳ 需新建 |
| **`/methodology`** | 数据 ▾ → 方法论 | ⏳ 需新建 |
| **`/customers`** | 客户(一级直链) | ⏳ 需新建 |
| **`/pricing`** | 定价(一级直链) | ⏳ 需新建 |
| **`/reports`** | 资源 ▾ → 白皮书/报告 | ⏳ 需新建 |
| **`/developers`** | 资源 ▾ → 开发者文档 | ⏳ 需新建 |
| **`/changelog`** | 资源 ▾ → 产品更新 | ⏳ 需新建 |
| **`/signup`** 或外链 | 主 CTA "开始试用" | ⏳ 需建注册流(如果要自助) |

**优雅降级策略:**

新导航**先上线**(只改 `lib/site.ts` 和 mega menu 组件),指向的目标页**逐个补齐**。在目标页还没有的过渡期:
- 还没建的二级链接保留在 mega menu 里,**hover 出 `[ 即将上线 ]` tooltip**(沿用 Phase 12 产品 mega menu satellite cell 的 frozen pattern)
- 已建好的页面正常跳转
- 这样用户能"看到全图"(知道我们的能力边界),同时不会点到 404

---

## 总结 · 一页纸版

> **从 [产品 / 解决方案 / 行业洞察 / 关于] 改成 [产品 / 数据 / 客户 / 定价 / 资源]。**
>
> **从单 CTA [联系销售] 改成双 CTA — 顶部 [开始试用](自助)+ 内页 [联系销售](企业)。**
>
> 改动只是 `lib/site.ts` + 2 个新 mega menu 组件,不到 1 天的工作。
>
> 但带来的结构性改进是:**数据品牌身份立住了 + 漏斗中段不再漏血 + 自助型客户不再被拒之门外**。
